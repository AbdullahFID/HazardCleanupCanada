---
import {
  Phone, Shield, Clock, Award, ChevronDown,
  ShieldCheck, Droplets, HeartPulse, Biohazard,
  CheckCircle, ArrowRight, Star, MapPin, Mail, Home,
  Briefcase, Users, MessageCircle, Zap, UserCheck,
  Microscope, ChevronLeft, ChevronRight,
  Instagram, HelpCircle,
  Siren, Building2, TestTube, BadgeCheck, Share2, Car, Flame, Skull,
} from "@lucide/astro";
---

<html lang="en-CA" class="h-full scroll-smooth" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

    <!-- ======= COMPREHENSIVE SEO ======= -->
    <title>Hazard Cleanup Canada | 24/7 Biohazard & Crime Scene Cleanup Services Ontario | Trauma Cleanup Specialists</title>
    <meta name="description" content="Professional 24/7 biohazard, crime scene, trauma & suicide cleanup services across Ontario. Discrete, certified technicians. Call +1 (647) 227-1443. GTA-based, Ontario-wide coverage." />
    <meta name="keywords" content="biohazard cleanup ontario, crime scene cleanup toronto, trauma cleanup gta, suicide cleanup services, blood cleanup professionals, discrete cleanup services, hazmat cleanup ontario, death cleanup services, contamination cleanup, emergency cleanup ontario, certified biohazard technicians, trauma scene restoration, forensic cleaning company, decomposition cleanup, homicide cleanup services, unattended death cleaning, after death cleaning service, accident cleanup services, hoarding cleanup service, bodily fluid cleanup, infectious disease cleanup, pathogen removal services, odor removal specialists, 24/7 emergency biohazard response, crime scene cleaners near me, biohazard remediation mississauga, trauma scene cleanup ottawa, blood spill cleanup hamilton, suicide scene cleaning kitchener, death scene cleanup london ontario, forensic cleaners waterloo, biohazard removal company brampton, hazmat team ontario, decontamination services toronto, biological hazard cleanup, sharps disposal service, needle cleanup ontario, drug lab cleanup, meth lab decontamination, fentanyl cleanup services, tear gas cleanup, pepper spray decontamination, industrial accident cleanup, workplace accident cleaning, vehicle decontamination services, car accident cleanup, blood borne pathogen cleaning, hepatitis cleanup, HIV contamination cleaning, MRSA decontamination, C-diff cleanup services, covid-19 disinfection, pandemic cleaning services, police scene cleanup, coroner scene cleaning, medical waste disposal ontario, human waste cleanup, sewage backup cleaning, extreme cleaning services, gross filth cleanup, professional sanitization services, biohazard waste disposal, regulated medical waste, red bag waste disposal, biological waste management, trauma cleaning specialists, compassionate cleanup services, discrete biohazard removal, confidential cleanup services, insurance approved cleanup, certified trauma cleaners, IICRC certified cleanup, licensed biohazard technicians, bonded cleanup company, insured cleanup services, commercial biohazard cleaning, residential trauma cleanup, property restoration services, odour neutralization, ATP testing services, discreet cleanup ontario, unmarked vehicle service, private cleanup services, sensitive scene cleaning, dignity restoration services, estate cleanup services, biohazard cleaning cost ontario, emergency response team, rapid response cleanup, same day cleanup service, after hours cleanup, weekend emergency service, holiday cleanup services forensic cleaning toronto, pathological waste disposal ontario, WHMIS Class D Division 3, Canadian Environmental Protection Act Regulation 347, touchless decontamination, WHO certified cleanup, Health Canada approved biohazard, gross filth cleanup toronto, estate cleanup services ontario, viral outbreak cleanup, post-ETF cleanup, emergency task force decontamination, free consultation biohazard, free quote crime scene, insurance direct billing cleanup, infection control treatment, surface air decontamination, licensed medical waste transporter, federal regulations compliance, 40 years experience cleanup, nationwide biohazard service canada, 5-step disinfection process, Canadian Health Care Engineering Society, rapid defense sanitization, infectious materials removal, bloodborne pathogen cleanup, property restoration ontario, 60 minute response time, asbestos mould lead abatement" />    
    <link rel="canonical" href="https://hazardcleanup.ca/" />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    <meta name="googlebot" content="index, follow" />
    <meta name="theme-color" content="#DC2626" />
    <meta name="msapplication-TileColor" content="#DC2626" />
    <link rel="icon" href="/Logo.png" />
    <link rel="apple-touch-icon" href="/Logo.png" />

    <!-- Language & Geographic -->
    <meta name="language" content="English" />
    <meta name="geo.region" content="CA-ON" />
    <meta name="geo.placename" content="Ontario, Canada" />
    <meta name="geo.position" content="43.6532;-79.3832" />
    <meta name="ICBM" content="43.6532, -79.3832" />
    <link rel="preconnect" href="https://challenges.cloudflare.com" crossorigin>

    <!-- Open Graph Enhanced -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Hazard Cleanup Canada" />
    <meta property="og:title" content="Hazard Cleanup Canada | 24/7 Discrete Biohazard & Crime Scene Cleanup Ontario" />
    <meta property="og:description" content="Professional biohazard, crime scene & trauma cleanup services across Ontario. Discrete, certified, 24/7 emergency response. Call +1 (647) 227-1443." />
    <meta property="og:url" content="https://hazardcleanup.ca/" />
    <meta property="og:locale" content="en_CA" />
    <meta property="og:image" content="https://hazardcleanup.ca/Logo.png" />
    <meta property="og:image:alt" content="Hazard Cleanup Canada - Professional Biohazard Cleanup Services" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter Enhanced -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Hazard Cleanup Canada | 24/7 Discrete Biohazard Cleanup Ontario" />
    <meta name="twitter:description" content="Professional biohazard, crime scene & trauma cleanup. Discrete, certified, 24/7. Call +1 (647) 227-1443." />
    <meta name="twitter:image" content="https://hazardcleanup.ca/Logo.png" />
    <meta name="twitter:site" content="@hazardcleanupca" />

    <!-- Enhanced JSON-LD -->
    <script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": "https://hazardcleanup.ca/#website",
      "url": "https://hazardcleanup.ca/",
      "name": "Hazard Cleanup Canada",
      "inLanguage": "en-CA",
      "potentialAction": {
        "@type": "SearchAction",
        "target": "https://hazardcleanup.ca/?q={search_term_string}",
        "query-input": "required name=search_term_string"
      }
    },
{
  "@type": "Service",
  "serviceType": [
    "Forensic Cleaning Services",
    "Pathological Waste Disposal",
    "Post-ETF Decontamination",
    "Infection Control Treatment",
    "WHO Certified Decontamination",
    "WHMIS Class D Division 3 Cleanup",
    "Estate Cleanup Services",
    "Gross Filth Remediation",
    "Viral Outbreak Response",
    "Emergency Task Force Cleanup"
  ],
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "CAD",
    "name": "Free Consultation & Quote",
    "description": "24/7 free consultation and estimation services"
  },
  "hasOfferCatalog": {
    "@type": "OfferCatalog",
    "name": "Biohazard Cleanup Services",
    "itemListElement": [
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Insurance Direct Billing",
          "description": "We work directly with your insurance provider"
        }
      }
    ]
  },
  "memberOf": [
    {"@type": "Organization", "name": "IICRC"},
    {"@type": "Organization", "name": "Canadian Environmental Protection Agency"}
  ],
  "knowsAbout": [
    "Canadian Environmental Protection Act Regulation 347",
    "WHMIS Classification System",
    "Health Canada Guidelines",
    "WHO Decontamination Protocols"
  ]
},
    {
      "@type": ["Organization","ProfessionalService","LocalBusiness"],
      "@id": "https://hazardcleanup.ca/#org",
      "name": "Hazard Cleanup Canada",
      "alternateName": "HCC",
      "url": "https://hazardcleanup.ca/",
      "logo": "https://hazardcleanup.ca/Logo.png",
      "image": "https://hazardcleanup.ca/og-card-1200x630.jpg",
      "description": "24/7 biohazard, crime scene, trauma and suicide cleanup across Ontario. Discreet, certified technicians with rapid emergency response.",
      "email": "help@hazardcleanup.ca",
      "telephone": "+1-647-227-1443",
      "priceRange": "$$",
      "sameAs": ["https://www.instagram.com/hazardcleanupca"],
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Mississauga",
        "addressRegion": "ON",
        "postalCode": "L5B 1A1",
        "addressCountry": "CA"
      },
      "areaServed": [
        {"@type":"AdministrativeArea","name":"Ontario"},
        {"@type":"City","name":"Toronto"},
        {"@type":"City","name":"Mississauga"},
        {"@type":"City","name":"Ottawa"},
        {"@type":"City","name":"Hamilton"}
      ],
      "openingHoursSpecification": [{
        "@type":"OpeningHoursSpecification",
        "dayOfWeek":["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"],
        "opens":"00:00","closes":"23:59"
      }],
      "geo": {"@type":"GeoCoordinates","latitude":43.6532,"longitude":-79.3832},
      "makesOffer": [{
        "@type": "Offer",
        "itemOffered": {"@type":"Service","name":"Crime Scene Cleanup"}
      },{
        "@type": "Offer",
        "itemOffered": {"@type":"Service","name":"Biohazard Remediation"}
      }],
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "5.0",
        "reviewCount": "3"
      },
      "contactPoint": [{
        "@type": "ContactPoint",
        "telephone": "+1-647-227-1443",
        "contactType": "customer service",
        "areaServed": "CA-ON",
        "availableLanguage": ["English"]
      }]
    },
    {
      "@type": "FAQPage",
      "@id": "https://hazardcleanup.ca/#faq",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "What types of scenes do you clean?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "We handle biohazard and trauma scenes including crime scenes, unattended deaths, suicides, accidents, blood spills, disease outbreaks, hoarding situations, drug labs, vehicle contamination, and more."
          }
        },
        {
          "@type": "Question",
          "name": "How discrete is your service?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "We use unmarked vehicles, discrete uniforms, and share information strictly on a need-to-know basis."
          }
        },
        {
          "@type": "Question",
          "name": "What areas do you serve?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "We're GTA-based with Ontario-wide coverage and rapid emergency response."
          }
        },
        {
          "@type": "Question",
          "name": "Who cleans up after someone dies?",
          "acceptedAnswer": {"@type": "Answer", "text": "Professional biohazard cleanup companies..."}
        },
        {
          "@type": "Question", 
          "name": "How much does crime scene cleanup cost in Ontario?",
          "acceptedAnswer": {"@type": "Answer", "text": "Costs vary based on scene complexity..."}
        },
        {
          "@type": "Question",
          "name": "Who pays for biohazard cleanup?",
          "acceptedAnswer": {"@type": "Answer", "text": "Often covered by homeowner's insurance..."}
        }
      ],
      "makesOffer": [
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Crime Scene Cleanup"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Biohazard Remediation"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Forensic Cleaning"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Decomposition Cleanup"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Homicide Scene Cleaning"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Unattended Death Cleanup"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Blood Spill Cleanup"}},
        {"@type": "Offer", "itemOffered": {"@type":"Service","name":"Infectious Disease Decontamination"}}
      ]
    }
  ]
}
    </script>

    <meta name="geo.placename" content="Toronto, Mississauga, Ottawa, Hamilton, London, Kitchener, Waterloo, Brampton, Ontario, Canada" />
<meta name="local.keywords" content="biohazard cleanup toronto downtown, crime scene cleaning mississauga, trauma cleanup scarborough, biohazard services etobicoke, death cleanup north york, blood cleanup vaughan, suicide cleanup richmond hill, forensic cleaning markham" />
<meta name="industry" content="Biohazard Remediation, Environmental Services, Emergency Response, Specialized Cleaning" />
<meta name="services" content="Crime Scene Cleanup, Trauma Scene Remediation, Biohazard Removal, Death Scene Cleaning, Blood Cleanup, Decomposition Remediation" />

    <!-- Cloudflare Turnstile -->
    <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
    
  </head>

<body class="relative min-h-screen antialiased overflow-x-hidden bg-[#0b1220]">
  <!-- Add after your body tag -->
<div itemscope itemtype="https://schema.org/EmergencyService" style="display:none">
  <meta itemprop="name" content="Hazard Cleanup Canada Emergency Response" />
  <meta itemprop="availableLanguage" content="en-CA, fr-CA" />
  <meta itemprop="priceRange" content="$$-$$$" />
  <meta itemprop="paymentAccepted" content="Cash, Credit Card, Insurance, Direct Billing" />
  <meta itemprop="currenciesAccepted" content="CAD" />
</div>

<!-- Microdata for trust signals -->
<div style="position:absolute;left:-9999px">
  <span itemprop="award">Health Canada Approved Contractor 2024</span>
  <span itemprop="certification">IICRC S540 Certified</span>
  <span itemprop="certification">Canadian Environmental Protection Act Compliant</span>
  <span itemprop="yearsInOperation">20+</span>
</div>
  <div aria-hidden="true" class="sr-only">
  <h2>Forensic Cleaning Services Ontario</h2>
  <h2>After Death Cleaning Company Toronto</h2>
  <h2>Decomposition Cleanup Specialists GTA</h2>
</div>
    <div aria-hidden
       class="fixed inset-0 -z-10
              bg-[radial-gradient(1200px_800px_at_92%_45%,rgba(220,38,38,.24),transparent_60%)]">
  </div>
    <!-- Skip Navigation -->
    <a href="#home" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-red-600 text-white px-4 py-2 rounded-lg z-50 transition-all">Skip to main content</a>

    <!-- ======= DESKTOP NAV ======= -->
    <nav id="navbar" class="hidden lg:block fixed top-0 left-0 right-0 z-50 transition-all duration-500">
      <div class="mx-auto max-w-7xl px-6 py-4">
        <div class="glass-nav flex items-center justify-between px-6 py-4 rounded-2xl shadow-2xl">
          <a href="#home" class="flex items-center gap-3 group">
            <div class="w-12 h-12 rounded-2xl bg-white ring-1 ring-black/10 flex items-center justify-center shadow-lg group-hover:scale-105 transition-transform">
              <img src="/Logo.png" alt="Hazard Cleanup Canada logo" class="w-8 h-8" />
            </div>

            <div class="flex flex-col">
              <span class="font-extrabold text-lg tracking-tight text-white">Hazard Cleanup Canada</span>
              <span class="text-xs text-red-400">24/7 Emergency Response</span>
            </div>
          </a>

          <div class="flex items-center gap-1 relative">
            <div id="nav-indicator" class="absolute h-10 rounded-full bg-gradient-to-r from-red-500/20 to-red-600/20 border border-red-500/30 transition-all duration-300"></div>
            <a class="nav-link" data-section="home" href="#home">Home</a>
            <a class="nav-link" data-section="services" href="#services">Services</a>
            <a class="nav-link" data-section="about" href="#about">About</a>
            <a class="nav-link" data-section="faq" href="#faq">FAQ</a>
            <a class="nav-link" data-section="contact" href="#contact">Contact</a>
          </div>

          <div class="flex items-center gap-3">
            <a href="tel:+16472271443" class="btn-primary flex items-center gap-2" aria-label="Call +1 647 227 1443">
              <Phone class="w-4 h-4" /> 
              <span class="font-semibold">+1 (647) 227-1443</span>
            </a>
          </div>
        </div>
      </div>
    </nav>

<!-- ======= MOBILE BOTTOM PILL NAV ======= -->
<nav id="mobile-bottom-nav"
     class="lg:hidden fixed inset-x-0 z-[60] pointer-events-none"
     style="bottom: calc(env(safe-area-inset-bottom) + 12px);">
  <div class="flex justify-center">
    <div class="mobile-pill-nav pointer-events-auto" id="mobile-pill">
      <a href="#home"      class="bottom-nav-icon"  data-section="home"      aria-label="Home"><Home class="w-6 h-6" /></a>
      <a href="#services"  class="bottom-nav-icon"  data-section="services"  aria-label="Services"><Briefcase class="w-6 h-6" /></a>
      <a href="tel:+16472271443" class="bottom-nav-icon call" aria-label="Call Now"><Phone class="w-7 h-7" /></a>
      <a href="#faq"       class="bottom-nav-icon"  data-section="faq"       aria-label="FAQ"><HelpCircle class="w-6 h-6" /></a>
      <a href="#contact"   class="bottom-nav-icon"  data-section="contact"   aria-label="Contact"><MessageCircle class="w-6 h-6" /></a>
    </div>
  </div>
</nav>



    <!-- ======= HERO ======= -->
    <section id="home" class="section px-6 pt-32 lg:pt-40 pb-20">
      <div class="mx-auto max-w-6xl text-center">
        <div class="inline-flex items-center gap-2 badge-24h mb-8 animate-pulse">
          <Siren class="w-4 h-4" />
          <span class="font-semibold">24/7 Emergency Response</span>
        </div>
        
        <h1 class="hero-title mb-8">
          <span class="block text-white">Restoring Safety,</span>
          <span class="block bg-gradient-to-r from-red-400 via-red-500 to-red-600 bg-clip-text text-transparent">Rebuilding Lives</span>
        </h1>
        
        <p class="lead max-w-3xl mx-auto mb-10">
          GTA-based, serving <strong class="text-red-400">all of Ontario</strong> with certified, <strong class="text-red-400">discrete</strong> biohazard & trauma cleanup services. Professional, compassionate, and confidential.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-16">
          <a href="tel:+16472271443" class="btn-primary text-lg px-8 py-4">
            <Phone class="w-5 h-5" /> 
            Call Now: +1 (647) 227-1443
          </a>
          <a href="#services" class="btn-secondary text-lg px-8 py-4">
            Our Services 
            <ArrowRight class="w-5 h-5" />
          </a>
        </div>

        <div class="grid grid-cols-2 lg:grid-cols-4 gap-6 max-w-4xl mx-auto">
          <div class="stat-card">
            <ShieldCheck class="w-8 h-8 text-red-400 mx-auto mb-3" />
            <p class="font-semibold text-white">Licensed & Insured</p>
            <p class="text-xs text-gray-400 mt-1">Fully certified technicians</p>
          </div>
          <div class="stat-card">
            <Clock class="w-8 h-8 text-red-400 mx-auto mb-3" />
            <p class="font-semibold text-white">Rapid Response</p>
            <p class="text-xs text-gray-400 mt-1">Emergency dispatch</p>
          </div>
          <div class="stat-card">
            <Award class="w-8 h-8 text-red-400 mx-auto mb-3" />
            <p class="font-semibold text-white">IICRC Certified</p>
            <p class="text-xs text-gray-400 mt-1">Industry standards</p>
          </div>
          <div class="stat-card">
            <BadgeCheck class="w-8 h-8 text-red-400 mx-auto mb-3" />
            <p class="font-semibold text-white">Ontario-Wide</p>
            <p class="text-xs text-gray-400 mt-1">GTA-based coverage</p>
          </div>
        </div>
      </div>

      <div class="text-center mt-12">
        <ChevronDown class="w-8 h-8 text-gray-400 mx-auto animate-bounce" />
      </div>
    </section>

    <!-- ======= SERVICES ======= -->
    <section id="services" class="section px-6 py-20">
  <div class="mx-auto max-w-7xl">
    <div class="text-center mb-16">
      <div class="chip mb-6">
        <Briefcase class="w-4 h-4 text-red-400" />
        <span>Specialized Cleanup Services</span>
      </div>
      <h2 class="h2 text-white mb-6">Professional, Discrete Services</h2>
      <p class="lead max-w-2xl mx-auto">We handle sensitive scenes with compassion, privacy and precision across all of Ontario.</p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8" id="svc-grid">
      <!-- Unattended Death (FLIP) -->
      <div class="service-card flip-card">
        <div class="flip-inner">
          <!-- FRONT -->
          <div class="flip-face front">
            <div class="avatar-indigo mb-6">
              <Skull class="w-8 h-8 text-white" />
            </div>
            <h3 class="text-xl font-bold text-white mb-3">Unattended Death Cleanup</h3>
            <p class="text-gray-300 mb-6">Dignified decomposition remediation with complete privacy and respect.</p>

            <button class="learn-more-btn flip-toggle" aria-expanded="false">
              <span>Learn More</span>
              <ArrowRight class="w-4 h-4" />
            </button>
          </div>

          <!-- BACK -->
          <div class="flip-face back">
            <h4 class="font-semibold text-white mb-4">What's Included:</h4>
            <ul class="service-checklist space-y-3 mb-6">
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Complete affected material removal</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Advanced odor treatment & neutralization</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Structural penetration assessment</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Discrete, unmarked vehicle service</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Final inspection & property handover</li>
            </ul>

            <div class="back-footer">
              <button type="button" class="back-btn group flip-back">
                <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
                <span class="font-semibold">Back</span>
              </button>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Crime Scene (already flipped) -->
      <div class="service-card flip-card">
        <div class="flip-inner">
          <!-- FRONT -->
          <div class="flip-face front">
            <div class="avatar-red mb-6">
              <Droplets class="w-8 h-8 text-white" />
            </div>
            <h3 class="text-xl font-bold text-white mb-3">Crime Scene Cleanup</h3>
            <p class="text-gray-300 mb-6">
              Homicide, assault, trauma scenes - evidence-aware, respectful, private remediation.
            </p>

            <button class="learn-more-btn flip-toggle" aria-expanded="false">
              <span>Learn More</span>
              <ArrowRight class="w-4 h-4" />
            </button>
          </div>

          <!-- BACK -->
          <div class="flip-face back">
            <h4 class="font-semibold text-white mb-4">What's Included:</h4>
            <ul class="service-checklist space-y-3 mb-6">
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Complete blood & bodily fluid removal</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Scene isolation & contamination control</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Hospital-grade disinfection & odor elimination</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Proper biohazard packaging & disposal</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Documentation & photography (if requested)</li>
            </ul>

            <div class="back-footer">
              <button type="button" class="back-btn group flip-back">
                <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
                <span class="font-semibold">Back</span>
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Biohazard (FLIP) -->
      <div class="service-card flip-card">
        <div class="flip-inner">
          <!-- FRONT -->
          <div class="flip-face front">
            <div class="avatar-amber mb-6">
              <Biohazard class="w-8 h-8 text-white" />
            </div>
            <h3 class="text-xl font-bold text-white mb-3">Biohazard Remediation</h3>
            <p class="text-gray-300 mb-6">Safe removal of infectious materials in homes, vehicles, or commercial facilities.</p>

            <button class="learn-more-btn flip-toggle" aria-expanded="false">
              <span>Learn More</span>
              <ArrowRight class="w-4 h-4" />
            </button>
          </div>

          <!-- BACK -->
          <div class="flip-face back">
            <h4 class="font-semibold text-white mb-4">What's Included:</h4>
            <ul class="service-checklist space-y-3 mb-6">
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Pathogen source identification & removal</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> EPA-approved antimicrobial treatments</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Full PPE protocols & containment barriers</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Air quality monitoring & HEPA filtration</li>
              <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Clearance testing & certification</li>
            </ul>

            <div class="back-footer">
              <button type="button" class="back-btn group flip-back">
                <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
                <span class="font-semibold">Back</span>
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- More services (hidden initially) -->
      <div id="more-services" class="contents hidden">

        <!-- Suicide Cleanup (FLIP) -->
        <div class="service-card flip-card">
          <div class="flip-inner">
            <!-- FRONT -->
            <div class="flip-face front">
              <div class="avatar-rose mb-6">
                <HeartPulse class="w-8 h-8 text-white" />
              </div>
              <h3 class="text-xl font-bold text-white mb-3">Suicide Cleanup</h3>
              <p class="text-gray-300 mb-6">Compassionate, discrete service for families during difficult times.</p>

              <button class="learn-more-btn flip-toggle" aria-expanded="false">
                <span>Learn More</span>
                <ArrowRight class="w-4 h-4" />
              </button>
            </div>

            <!-- BACK -->
            <div class="flip-face back">
              <h4 class="font-semibold text-white mb-4">What's Included:</h4>
              <ul class="service-checklist space-y-3 mb-6">
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Completely discrete arrival & service</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Trauma scene remediation</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Compassionate family support</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Personal property care & protection</li>
              </ul>

              <div class="back-footer">
                <button type="button" class="back-btn group flip-back">
                  <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
                  <span class="font-semibold">Back</span>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Vehicle Decontamination (FLIP) -->
        <div class="service-card flip-card">
          <div class="flip-inner">
            <!-- FRONT -->
            <div class="flip-face front">
              <div class="avatar-slate mb-6">
                <Car class="w-8 h-8 text-white" />
              </div>
              <h3 class="text-xl font-bold text-white mb-3">Vehicle Decontamination</h3>
              <p class="text-gray-300 mb-6">Comprehensive cleanup for cars, trucks, and commercial vehicles.</p>

              <button class="learn-more-btn flip-toggle" aria-expanded="false">
                <span>Learn More</span>
                <ArrowRight class="w-4 h-4" />
              </button>
            </div>

            <!-- BACK -->
            <div class="flip-face back">
              <h4 class="font-semibold text-white mb-4">What's Included:</h4>
              <ul class="service-checklist space-y-3 mb-6">
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Complete interior teardown as needed</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Specialized automotive cleaning agents</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Odor elimination & air treatment</li>
                <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Professional reassembly & detailing</li>
              </ul>

              <div class="back-footer">
                <button type="button" class="back-btn group flip-back">
                  <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
                  <span class="font-semibold">Back</span>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Disease Outbreak Cleaning (FLIP) -->
        
        <!-- 🔥 Fire & Flood Disaster Restoration -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-rose mb-6">
        <Flame class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Fire & Flood Disaster Restoration</h3>
      <p class="text-gray-300 mb-6">
        Emergency water removal, structural drying, smoke/soot cleanup, and full deodorization.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Rapid water extraction & structural drying</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Smoke/soot removal & HEPA air filtration</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Moisture mapping, mold prevention & monitoring</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Debris removal & odor neutralization</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Rebuild coordination (drywall, flooring, paint)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Photos & documentation for insurance</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- 🏠 Residential Restoration & Cleaning -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-slate mb-6">
        <Building2 class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Residential Restoration & Cleaning</h3>
      <p class="text-gray-300 mb-6">
        Professional repairs and deep cleaning — drywall, trim, flooring, stain & odor treatment.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Drywall patching, trim & minor carpentry</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Deep cleaning & disinfection of affected rooms</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Flooring repairs & spot replacement</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Stain removal & localized odor control</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Contents wipe-down & pack-out (as needed)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Touch-up painting & proper disposal</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- 🧫 Disease Outbreak Remediation -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-green mb-6">
        <Microscope class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Disease Outbreak Remediation</h3>
      <p class="text-gray-300 mb-6">
        Targeted decontamination for viral & bacterial exposures with return-to-service guidance.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Site assessment, risk zoning & PPE protocols</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Health-Canada/EPA-listed disinfectants</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> High-touch surface disinfection workflow</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Fogging/electrostatic application when appropriate</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> HEPA filtration & ventilation improvements</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Clearance guidance & documentation</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- ⚗️ Drug Lab / Needle Cleanup -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-amber mb-6">
        <TestTube class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Drug Lab & Needle Cleanup</h3>
      <p class="text-gray-300 mb-6">
        Safe removal of sharps and chemical residues with controlled disposal and deodorization.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Needle & sharps search, removal & containers</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Chemical residue assessment & neutralization</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Porous material removal & compliant disposal</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> HVAC/vent cleaning & negative air (as needed)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Odor treatment (ozone/hydroxyl or fogging)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Chain-of-custody & service documentation</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- 🐾 Wildlife Cleanup & Decontamination -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-indigo mb-6">
        <Shield class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Wildlife Cleanup & Decontamination</h3>
      <p class="text-gray-300 mb-6">
        Humane cleanup of droppings and nests with exclusion, deodorizing, and disinfection.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Removal of droppings, nests & contaminated debris</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> HEPA vacuuming & enzyme-based disinfection</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Odor/pheromone neutralization</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Entry-point identification & sealing (exclusion)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Insulation removal/replacement if affected</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Attic/crawlspace sanitation & final inspection</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>

<!-- ✨ Odour Removal & Deodorization -->
<div class="service-card flip-card">
  <div class="flip-inner">
    <!-- FRONT -->
    <div class="flip-face front">
      <div class="avatar-red mb-6">
        <Zap class="w-8 h-8 text-white" />
      </div>
      <h3 class="text-xl font-bold text-white mb-3">Odour Removal & Deodorization</h3>
      <p class="text-gray-300 mb-6">
        Source-based odour elimination for smoke, protein, pets, decomposition and more.
      </p>

      <button class="learn-more-btn flip-toggle" aria-expanded="false">
        <span>Learn More</span>
        <ArrowRight class="w-4 h-4" />
      </button>
    </div>

    <!-- BACK -->
    <div class="flip-face back">
      <h4 class="font-semibold text-white mb-4">What's Included:</h4>
      <ul class="service-checklist space-y-3 mb-6">
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Odour source identification & removal</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Enzyme, oxidizer & smoke/protein treatments</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Ozone or hydroxyl generation (as appropriate)</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Thermal fogging & surface sealing where needed</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> HVAC/duct deodorization & filter changes</li>
        <li><CheckCircle class="w-5 h-5 text-emerald-400" /> Final verification & customer walk-through</li>
      </ul>

      <div class="back-footer">
        <button type="button" class="back-btn group flip-back">
          <ChevronLeft class="w-4 h-4 transition-transform duration-150 group-hover:-translate-x-1" />
          <span class="font-semibold">Back</span>
        </button>
      </div>
    </div>
  </div>
</div>


      </div> <!-- /#more-services -->
    </div>

    <!-- Show More/Less Button -->
    <div class="text-center mt-12">
      <button id="services-toggle" class="pill-button">
        <span id="toggle-text">Show More Services</span>
        <ChevronDown id="toggle-icon" class="w-5 h-5 transition-transform duration-300" />
      </button>
    </div>
  </div>
</section>


    <!-- ======= ABOUT ======= -->
    <section id="about" class="section px-6 py-20">
      <div class="mx-auto max-w-7xl">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <div class="chip mb-6">
              <Users class="w-4 h-4 text-red-400" /> 
              <span>About Us</span>
            </div>
            <h2 class="h2 text-white mb-6">Trusted, Discrete Remediation Specialists</h2>
            <p class="text-gray-300 text-lg mb-8">
              With over 20 years of field experience, our certified technicians handle the most sensitive scenes with unmatched care and professionalism. Your comfort, safety, and privacy drive everything we do.
            </p>

            <div class="space-y-6">
              <div class="about-feature">
                <div class="feature-icon">
                  <UserCheck class="w-6 h-6 text-white" />
                </div>
                <div>
                  <h4 class="font-semibold text-white mb-2">IICRC Certified Professionals</h4>
                  <p class="text-gray-300">Extensively trained crew following strict safety protocols and documented workflows for every project.</p>
                </div>
              </div>

              <div class="about-feature">
                <div class="feature-icon">
                  <BadgeCheck class="w-6 h-6 text-white" />
                </div>
                <div>
                  <h4 class="font-semibold text-white mb-2">Discrete Service All Over Ontario</h4>
                  <p class="text-gray-300">Rapid response across the GTA with comprehensive service coverage throughout Ontario. Unmarked vehicles, discrete uniforms.</p>
                </div>
              </div>

              <div class="about-feature">
                <div class="feature-icon">
                  <HeartPulse class="w-6 h-6 text-white" />
                </div>
                <div>
                  <h4 class="font-semibold text-white mb-2">Compassion & Complete Privacy</h4>
                  <p class="text-gray-300">Every interaction is handled with utmost discretion. Need-to-know communication protects your privacy completely.</p>
                </div>
              </div>
            </div>

            <div class="mt-8">
              <div class="badge-24h inline-flex">
                <Siren class="w-4 h-4" />
                <span class="font-semibold">24/7 Emergency Response Team</span>
              </div>
            </div>
          </div>

          <!-- Stats & Reviews -->
          <div class="glass-dark rounded-3xl p-8">
            <div class="grid grid-cols-2 gap-8 text-center mb-8">
              <div>
                <div class="text-4xl font-extrabold text-red-400 mb-2">500+</div>
                <p class="text-gray-300">Projects Completed</p>
              </div>
              <div>
                <div class="text-4xl font-extrabold text-red-400 mb-2">24/7</div>
                <p class="text-gray-300">Emergency Response</p>
              </div>
              <div>
                <div class="text-4xl font-extrabold text-red-400 mb-2">100%</div>
                <p class="text-gray-300">Discrete Service</p>
              </div>
              <div>
                <div class="text-4xl font-extrabold text-red-400 mb-2">20+</div>
                <p class="text-gray-300">Years Experience</p>
              </div>
            </div>

            <!-- Reviews Carousel -->
            <div class="relative">
              <div class="overflow-hidden rounded-2xl">
                <div id="reviews-track" class="flex transition-transform duration-500 ease-in-out">
                  <div class="review-slide w-full flex-shrink-0">
                    <div class="review-card">
                      <div class="flex mb-3">
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                      </div>
                      <p class="text-gray-300 italic mb-4">“They arrived in under two hours with an unmarked van and quietly explained every step. They treated my father’s apartment with so much respect and kept us updated the whole time. By the next day there was no odour at all.”</p>
                      <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-400">Laura P., Etobicoke</span>
                        <MapPin class="w-4 h-4 text-gray-400" />
                      </div>
                    </div>
                  </div>

                  <div class="review-slide w-full flex-shrink-0">
                    <div class="review-card">
                      <div class="flex mb-3">
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                      </div>
                      <p class="text-gray-300 italic mb-4">“After a incident at our small shop, they sealed off the area, documented everything for our records, and were transparent about costs. Professional gear, calm crew, and the place looked normal again by evening.”</p>
                      <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-400">Ahmed R., Hamilton</span>
                        <MapPin class="w-4 h-4 text-gray-400" />
                      </div>
                    </div>
                  </div>

                  <div class="review-slide w-full flex-shrink-0">
                    <div class="review-card">
                      <div class="flex mb-3">
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                        <Star class="w-5 h-5 text-yellow-400 fill-current" />
                      </div>
                      <p class="text-gray-300 italic mb-4">“I needed my SUV decontaminated after a medical emergency. They removed the affected materials, treated the air, and checked back a week later. No lingering smell and you’d never know anything happened.”</p>
                      <div class="flex items-center justify-between">
                        <span class="text-sm text-gray-400">Jenna K., Ottawa</span>
                        <MapPin class="w-4 h-4 text-gray-400" />
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <button id="prev-review" class="review-nav-btn left-2">
                <ChevronLeft class="w-5 h-5" />
              </button>
              <button id="next-review" class="review-nav-btn right-2">
                <ChevronRight class="w-5 h-5" />
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ======= FAQ ======= -->
    <section id="faq" class="section px-6 py-20">
      <div class="mx-auto max-w-4xl">
        <div class="text-center mb-16">
          <div class="chip mb-6">
            <MessageCircle class="w-4 h-4 text-red-400" /> 
            <span>Frequently Asked Questions</span>
          </div>
          <h2 class="h2 text-white mb-6">What to Expect</h2>
          <p class="lead">Common questions about our services and process.</p>
        </div>

        <div class="space-y-4">
          <details class="faq-item">
            <summary class="faq-question">
              <span>What types of scenes do you clean?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>We handle all types of biohazard and trauma scenes including crime scenes, unattended deaths, suicides, accidents, blood spills, disease outbreaks, hoarding situations, drug labs, vehicle contamination, and more. Every situation is approached with complete professionalism and discretion.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>How discrete is your service?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>Complete discretion is our priority. We use unmarked vehicles, wear discrete uniforms, and share information strictly on a need-to-know basis. Our team understands the sensitive nature of our work and maintains the highest level of confidentiality.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>What areas do you serve?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>We are based in the Greater Toronto Area and provide comprehensive cleanup services throughout all of Ontario. We offer rapid emergency response across the GTA and can reach anywhere in Ontario within hours.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>How quickly can you respond?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>We provide 24/7 emergency response. Our team can typically arrive within 1-2 hours in the GTA, and we prioritize rapid response for all emergency situations throughout Ontario. Every minute matters in trauma cleanup.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>Are your technicians certified?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>Yes, all our technicians are IICRC-certified and extensively trained in biohazard remediation, bloodborne pathogen handling, and trauma scene cleanup. We maintain the highest industry standards and follow strict safety protocols.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>What about insurance coverage?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>Many clients choose to pay directly for our services. We can provide all necessary documentation for insurance claims if you decide to submit one. We'll work with you to ensure you have everything needed for your specific situation.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>How do you ensure complete decontamination?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>We use hospital-grade disinfectants, EPA-approved antimicrobial treatments, and follow strict decontamination protocols. Our process includes thorough cleaning, disinfection, odor elimination, and final clearance testing to ensure the area is completely safe.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary class="faq-question">
              <span>What should I do while waiting for your arrival?</span>
              <ChevronDown class="w-5 h-5 transition-transform duration-200" />
            </summary>
            <div class="faq-answer">
              <p>Do not attempt to clean anything yourself. Restrict access to the affected area, avoid touching any contaminated surfaces, and wait for our certified technicians. We'll guide you through any immediate safety steps over the phone if needed.</p>
            </div>
          </details>
        </div>
      </div>
    </section>

    <!-- ======= CONTACT ======= -->
    <section id="contact" class="section px-6 py-20">
      <div class="mx-auto max-w-7xl">
        <div class="text-center mb-16">
          <div class="chip mb-6">
            <Phone class="w-4 h-4 text-red-400" /> 
            <span>Get Help Now</span>
          </div>
          <h2 class="h2 text-white mb-6">24/7 Emergency Response</h2>
          <p class="lead">Professional help is just a phone call away. Discrete, compassionate, immediate response.</p>
        </div>

        <div class="grid lg:grid-cols-5 gap-12 items-start">
          <!-- Contact Info -->
          <div class="lg:col-span-2">
            <div class="space-y-6">
              <a href="tel:+16472271443" class="contact-method group">
                <div class="contact-icon">
                  <Phone class="w-6 h-6 text-white" />
                </div>
                <div>
                  <p class="text-sm text-gray-400 mb-1">Emergency Hotline</p>
                  <p class="text-xl font-bold text-white group-hover:text-red-400 transition-colors">+1 (647) 227-1443</p>
                  <p class="text-sm text-red-400">Available 24/7</p>
                </div>
              </a>

              <a href="mailto:help@hazardcleanup.ca" class="contact-method group">
                <div class="contact-icon">
                  <Mail class="w-6 h-6 text-white" />
                </div>
                <div>
                  <p class="text-sm text-gray-400 mb-1">Email</p>
                  <p class="font-semibold text-white group-hover:text-red-400 transition-colors">help@hazardcleanup.ca</p>
                  <p class="text-sm text-red-400">Confidential inquiries</p>
                </div>
              </a>

              <div class="contact-method">
                <div class="contact-icon">
                  <MapPin class="w-6 h-6 text-white" />
                </div>
                <div>
                  <p class="text-sm text-gray-400 mb-1">Service Area</p>
                  <p class="font-semibold text-white">All of Ontario</p>
                  <p class="text-sm text-red-400">GTA-based for rapid response</p>
                </div>
              </div>
            </div>

            <div class="mt-8">
              <div class="badge-local inline-flex">
                <BadgeCheck class="w-4 h-4" />
                <span>GTA-based • Trusted local team</span>
              </div>
            </div>

            <!-- Social Links -->
            <div class="mt-8 flex items-center gap-4">
              <a href="https://www.instagram.com/hazardcleanupca" class="social-btn" aria-label="Follow us on Instagram">
                <Instagram class="w-5 h-5" />
              </a>
              <button id="share-contact" class="social-btn" aria-label="Share our contact details">
                <Share2 class="w-5 h-5" />
              </button>
            </div>
          </div>

          <!-- Contact Form -->
          <div class="lg:col-span-3">
            <form id="contact-form" class="glass-dark rounded-3xl p-8" novalidate>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <div class="form-group">
                  <input 
                    type="text" 
                    id="name" 
                    name="name" 
                    class="form-input" 
                    placeholder="Your Name *" 
                    required 
                    autocomplete="name"
                  />
                </div>
                <div class="form-group">
                  <input 
                    type="tel" 
                    id="phone" 
                    name="phone" 
                    class="form-input" 
                    placeholder="Phone Number *" 
                    required 
                    autocomplete="tel"
                  />
                </div>
              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <div class="form-group">
                  <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-input" 
                    placeholder="Email (optional)" 
                    autocomplete="email"
                  />
                </div>
                <div class="form-group">
                  <select id="service" name="service" class="form-input">
                    <option value="">Select Service Needed</option>
                    <option value="crime-scene">Crime Scene Cleanup</option>
                    <option value="biohazard">Biohazard Remediation</option>
                    <option value="unattended-death">Unattended Death</option>
                    <option value="suicide">Suicide Cleanup</option>
                    <option value="disease-outbreak">Disease Outbreak</option>
                    <option value="vehicle">Vehicle Decontamination</option>
                    <option value="flood">Flood/Sewage Cleanup</option>
                    <option value="fire">Fire/Smoke Cleanup</option>
                    <option value="other">Other/Emergency</option>
                  </select>
                </div>
              </div>

              <div class="form-group mb-6">
                <textarea 
                  id="description" 
                  name="description" 
                  rows="4" 
                  class="form-input" 
                  placeholder="Brief description of the situation (optional but helpful for preparation)"
                ></textarea>
              </div>

              <!-- Honeypot field -->
              <input 
                type="text" 
                name="website" 
                class="hidden-field" 
                tabindex="-1" 
                autocomplete="off"
              />

              <!-- Turnstile -->
              <div class="mb-6">
                <div class="cf-turnstile"
                  data-sitekey="0x4AAAAAABvO7F9WJDTBwr1z"
                  data-callback="onTurnstileCallback"></div>

              </div>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                <button type="submit" class="btn-primary w-full justify-center text-lg py-4">
                   Submit Form
                </button>
                <a href="tel:+16472271443" class="btn-secondary w-full justify-center text-lg py-4">
                  Or Just Call Now
                </a>
              </div>

              <p class="text-center text-sm text-gray-400">
                All communications are confidential and private. Typical response within minutes.
              </p>

              <!-- Success Message -->
              <div id="form-success" class="hidden success-message">
                <div class="success-checkmark">
                  <svg viewBox="0 0 52 52" class="success-animation">
                    <circle cx="26" cy="26" r="25" fill="none"/>
                    <path fill="none" d="m14.1 27.2l7.1 7.2 16.7-16.8"/>
                  </svg>
                </div>
                <div class="text-center">
                  <h3 class="text-lg font-semibold text-white mb-2">Message Received</h3>
                  <p class="text-gray-300">We'll contact you within minutes. Thank you for reaching out.</p>
                </div>
              </div>
            </form>

            <!-- Google Maps -->
           <!-- Google Maps - LAZY LOADED -->
<div class="mt-8">
  <div class="glass-dark rounded-3xl p-2">
    <iframe
      title="Hazard Cleanup Canada Service Area - Ontario"
      width="100%" 
      height="300" 
      loading="lazy" 
      referrerpolicy="no-referrer-when-downgrade"
      src="data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%3E%3C/svg%3E"
      data-src="https://maps.google.com/maps?q=Mississauga%2C%20ON&z=9&t=m&output=embed&iwloc=near&hl=en"
      class="w-full rounded-2xl lazy-map"
      allowfullscreen>
    </iframe>
  </div>
  <p class="text-center text-xs text-gray-400 mt-2">
    * Outside the GTA, additional travel time and fees may apply for emergency response.
  </p>
</div>

<script>
  // Lazy load Google Maps on scroll
// Lazy load Google Maps on scroll
const mapObserver = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (!entry.isIntersecting) return;

    if (entry.target instanceof HTMLIFrameElement) {
      const iframe = entry.target;
      const src = iframe.getAttribute('data-src');
      if (src) {
        iframe.src = src;
        iframe.removeAttribute('data-src');
        mapObserver.unobserve(iframe);
      }
    }
  });
});

document.querySelectorAll('.lazy-map').forEach((iframe) => {
  mapObserver.observe(iframe);
});

</script>
          </div>
        </div>
      </div>
    </section>

    <!-- ======= FOOTER ======= -->
    <footer class="border-t border-gray-700/50 px-6 py-16">
      <div class="mx-auto max-w-7xl">
        <div class="grid md:grid-cols-3 gap-12 mb-12">
          <!-- Brand -->
          <div>
            <div class="flex items-center gap-3 mb-6">
              <div class="w-12 h-12 rounded-2xl bg-white ring-1 ring-black/10 flex items-center justify-center">
                <img src="/Logo.png" alt="HCC" class="w-8 h-8" />
              </div>
              <div>
                <span class="font-extrabold text-lg text-white brand-text">Hazard Cleanup Canada</span>
                <p class="text-sm text-red-400">24/7 Emergency Response</p>
              </div>
            </div>
            <p class="text-gray-400 mb-4">
              Professional, discrete biohazard and trauma cleanup services across Ontario. 
              From the makers of <em>Pristine Cleaning Solutions</em> — serving Ontario for over 20 years.
            </p>
          </div>

          <!-- Quick Links -->
          <div>
            <h4 class="font-semibold text-white mb-4">Quick Links</h4>
            <ul class="space-y-3">
              <li><a href="#services" class="footer-link">Our Services</a></li>
              <li><a href="#about" class="footer-link">About Us</a></li>
              <li><a href="#faq" class="footer-link">FAQ</a></li>
              <li><a href="#contact" class="footer-link">Contact</a></li>
              <li><a href="tel:+16472271443" class="footer-link">Emergency: +1 (647) 227-1443</a></li>
            </ul>
          </div>

          <!-- Share -->
          <div>
            <h4 class="font-semibold text-white mb-4">Share Our Details</h4>
            <button id="share-footer" class="btn-secondary mb-4">
              <Share2 class="w-4 h-4" /> 
              Quick Share
            </button>
            <p class="text-sm text-gray-400">
              Easily share our contact information with someone who needs help. 
              Includes name, phone number, and website.
            </p>
          </div>
        </div>

        <div class="border-t border-gray-700/50 pt-8 text-center">
          <div class="flex flex-col md:flex-row justify-between items-center gap-4">
            <p class="text-sm text-gray-400">
              © 2025 Hazard Cleanup Canada • GTA-based • Ontario-wide service
            </p>
            <p class="text-sm text-gray-400">
              Website by Abdullah Khan • 
              <a href="mailto:abdullahaviator13@gmail.com" class="footer-link">abdullahaviator13@gmail.com</a>
            </p>
          </div>
        </div>
      </div>
    </footer>

    <!-- ======= JAVASCRIPT ======= -->
     <script type="module" defer lang="ts">
  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      const href = this.getAttribute('href');
      if (href && href !== '#') {
        e.preventDefault();
        const target = document.querySelector(href);
        if (target) {
          const offsetTop = target.offsetTop - 100;
          window.scrollTo({
            top: offsetTop,
            behavior: 'smooth'
          });
        }
      }
    });
  });

  // Navigation active states and hide/show
  let lastScrollY = window.scrollY;
  let ticking = false;

  function updateNav() {
    const currentScrollY = window.scrollY;
    const navbar = document.getElementById('navbar');
    const mobileBottomNav = document.getElementById('mobile-bottom-nav');
    const mobilePill = document.getElementById('mobile-pill');

    // Hide/show nav on scroll
    if (currentScrollY > lastScrollY && currentScrollY > 100) {
      // Scrolling down
      navbar?.style.setProperty('transform', 'translateY(-100%)');
      mobileBottomNav?.classList.add('hide');
    } else {
      // Scrolling up
      navbar?.style.setProperty('transform', 'translateY(0)');
      if (mobileBottomNav?.classList.contains('hide')) {
        mobileBottomNav.classList.remove('hide');
        mobilePill?.classList.add('mobile-pop');
        setTimeout(()=> mobilePill?.classList.remove('mobile-pop'), 220);
      }
    }

    lastScrollY = currentScrollY;
    ticking = false;

    // Update active section
    updateActiveSection();
  }

  function requestTick() {
    if (!ticking) {
      requestAnimationFrame(updateNav);
      ticking = true;
    }
  }

  window.addEventListener('scroll', requestTick, { passive: true });

  // Update active navigation section
  function updateActiveSection() {
    const sections = document.querySelectorAll('section[id]');
    const scrollPos = window.scrollY + 120;

    sections.forEach(section => {
      const top = section.offsetTop;
      const bottom = top + section.offsetHeight;
      const id = section.getAttribute('id');

      if (scrollPos >= top && scrollPos < bottom) {
        // Update desktop nav
        document.querySelectorAll('.nav-link').forEach(link => {
          if (link.getAttribute('data-section') === id) {
            link.classList.add('active');
            // Update indicator
            const indicator = document.getElementById('nav-indicator');
            if (indicator) {
              indicator.style.width = link.offsetWidth + 'px';
              indicator.style.left = link.offsetLeft + 'px';
            }
          } else {
            link.classList.remove('active');
          }
        });
        // Update bottom pill nav active icon
        document.querySelectorAll('#mobile-bottom-nav .bottom-nav-icon').forEach(icon => {
          if (icon.getAttribute('data-section') === id) {
            icon.classList.add('active');
          } else {
            icon.classList.remove('active');
          }
        });
      }
    });
  }

  // Services toggle
  const servicesToggle = document.getElementById('services-toggle');
  const moreServices = document.getElementById('more-services');
  const toggleText = document.getElementById('toggle-text');
  const toggleIcon = document.getElementById('toggle-icon');

  servicesToggle?.addEventListener('click', () => {
    const isHidden = moreServices?.classList.contains('hidden');
    
    if (isHidden) {
      moreServices?.classList.remove('hidden');
      moreServices?.classList.add('animate-fade-in');
      toggleText.textContent = 'Show Less Services';
      toggleIcon?.style.setProperty('transform', 'rotate(180deg)');
    } else {
      moreServices?.classList.add('hidden');
      moreServices?.classList.remove('animate-fade-in');
      toggleText.textContent = 'Show More Services';
      toggleIcon?.style.setProperty('transform', 'rotate(0deg)');
    }
  });

  // Reviews carousel
  let currentReview = 0;
  const reviewsTrack = document.getElementById('reviews-track');
  const reviewSlides = document.querySelectorAll('.review-slide');
  const totalReviews = reviewSlides.length;

  function showReview(index) {
    currentReview = (index + totalReviews) % totalReviews;
    if (reviewsTrack) {
      reviewsTrack.style.transform = `translateX(-${currentReview * 100}%)`;
    }
  }

  document.getElementById('prev-review')?.addEventListener('click', () => {
    showReview(currentReview - 1);
  });

  document.getElementById('next-review')?.addEventListener('click', () => {
    showReview(currentReview + 1);
  });

  // Auto-advance reviews
  setInterval(() => {
    showReview(currentReview + 1);
  }, 5000);

  // FAQ accordion
  document.querySelectorAll('.faq-item').forEach(item => {
    const summary = item.querySelector('summary');
    const icon = summary?.querySelector('svg');
    
    item.addEventListener('toggle', () => {
      if (item.open) {
        icon?.style.setProperty('transform', 'rotate(180deg)');
      } else {
        icon?.style.setProperty('transform', 'rotate(0deg)');
      }
    });
  });

  // Share functionality
  async function shareContact() {
    const shareData = {
      title: 'Hazard Cleanup Canada',
      text: `Hazard Cleanup Canada
24/7 Emergency Biohazard Cleanup
Contact: Shayaan Ali
Phone: +1 (647) 227-1443
Website: hazardcleanup.ca

Professional, discrete cleanup services across Ontario.`
    };

    try {
      if (navigator.share) {
        await navigator.share(shareData);
      } else {
        await navigator.clipboard.writeText(shareData.text);
        showNotification('Contact details copied to clipboard!');
      }
    } catch (err) {
      console.log('Error sharing:', err);
    }
  }

  document.getElementById('share-contact')?.addEventListener('click', shareContact);
  document.getElementById('share-footer')?.addEventListener('click', shareContact);

  // Notification system
  function showNotification(message) {
    const notification = document.createElement('div');
    notification.className = 'fixed top-4 right-4 bg-green-600 text-white px-6 py-3 rounded-lg shadow-lg z-50 animate-slide-in';
    notification.textContent = message;
    document.body.appendChild(notification);

    setTimeout(() => {
      notification.remove();
    }, 3000);
  }

  // ===== CONTACT FORM WITH RATE LIMITING =====
  
  // IndexedDB helper for rate limiting
  const DB_NAME = 'hazard-cleanup-db';
  const STORE_NAME = 'submissions';
  const RATE_LIMIT_HOURS = 72;

  async function openDB() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(DB_NAME, 1);
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => resolve(request.result);
      
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        if (!db.objectStoreNames.contains(STORE_NAME)) {
          db.createObjectStore(STORE_NAME);
        }
      };
    });
  }

  async function getLastSubmission() {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const transaction = db.transaction([STORE_NAME], 'readonly');
      const store = transaction.objectStore(STORE_NAME);
      const request = store.get('lastSubmissionTime');
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => resolve(request.result || 0);
    });
  }

  async function setLastSubmission(timestamp) {
    const db = await openDB();
    return new Promise((resolve, reject) => {
      const transaction = db.transaction([STORE_NAME], 'readwrite');
      const store = transaction.objectStore(STORE_NAME);
      const request = store.put(timestamp, 'lastSubmissionTime');
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => resolve(true);
    });
  }

  // Turnstile callback
  window.onTurnstileCallback = function(token) {
    window.turnstileToken = token;
  };

  // Contact form submission → POSTS to /api/contact (Cloudflare Pages Function)
const contactForm = document.getElementById('contact-form');
const formSuccess  = document.getElementById('form-success');

contactForm?.addEventListener('submit', async (e) => {
  e.preventDefault();

  const formData    = new FormData(contactForm);
  const name        = (formData.get('name') || '').toString().trim();
  const phone       = (formData.get('phone') || '').toString().trim();
  const email       = (formData.get('email') || '').toString().trim();
  const service     = (formData.get('service') || '').toString();
  const description = (formData.get('description') || '').toString();
  const honeypot    = (formData.get('website') || '').toString().trim(); // hidden field

  // Honeypot → silently succeed
  if (honeypot) {
    showSuccessMessage();
    contactForm.reset();
    return;
  }

  // Basic required fields
  if (!name || !phone) {
    showNotification('Please fill in your name and phone number.');
    return;
  }

  // Rate limiting (72h) — falls back gracefully if IndexedDB unavailable
  try {
    const last = await getLastSubmission();
    if (last && (Date.now() - last) < RATE_LIMIT_HOURS * 3600_000) {
      showSuccessMessage();
      contactForm.reset();
      return;
    }
  } catch (err) {
    console.log('Rate limiting check failed:', err);
  }

  // Turnstile token from the callback
  const turnstileToken = window.turnstileToken || '';

  // Send to your Pages Function
  try {
    const resp = await fetch('/api/contact', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        name, phone, email, service, description,
        website: honeypot,           // keep the key name if your function checks it
        turnstileToken
      })
    });

    // Optional: read response for nicer UX
    if (!resp.ok) {
      let out = {};
      try { out = await resp.json(); } catch {}
      if (out?.error === 'turnstile_failed') {
        showNotification('Verification failed. Please check the Turnstile box and try again.');
      } else if (out?.error === 'missing_fields') {
        showNotification('Please fill in your name and phone number.');
      } else {
        showNotification('Thanks — we received your message.');
      }
    }
  } catch (err) {
    console.log('Form submission error:', err);
  } finally {
    try { await setLastSubmission(Date.now()); } catch {}
    showSuccessMessage();
    contactForm.reset();
    try { window.turnstile?.reset(); } catch {}
  }
});

 

  function showSuccessMessage() {
    contactForm.style.display = 'none';
    formSuccess?.classList.remove('hidden');
    
    setTimeout(() => {
      formSuccess?.classList.add('hidden');
      contactForm.style.display = 'block';
    }, 3000);
  }

  // Initialize page
  document.addEventListener('DOMContentLoaded', () => {
    updateActiveSection();
  });
</script>

<script>
  // Flip card handlers - FIXED VERSION
  function attachFlipEvents() {
    document.querySelectorAll('.flip-card').forEach(card => {
      const toggleBtn = card.querySelector('.flip-toggle');
      const backBtn = card.querySelector('.flip-back');

      toggleBtn?.addEventListener('click', () => {
        card.classList.add('is-flipped');
        toggleBtn.setAttribute('aria-expanded', 'true');
      });

      backBtn?.addEventListener('click', () => {
        card.classList.remove('is-flipped');
        const toggle = card.querySelector('.flip-toggle');
        toggle?.setAttribute('aria-expanded', 'false');
      });

      // ESC to go back - FIXED
      card.addEventListener('keydown', (e) => {
        if (e instanceof KeyboardEvent && e.key === 'Escape') {
          card.classList.remove('is-flipped');
          const toggle = card.querySelector('.flip-toggle');
          toggle?.setAttribute('aria-expanded', 'false');
          if (toggle instanceof HTMLElement) {
            toggle.focus();
          }
        }
      });
    });
  }

  // Size flip cards - FIXED
  function sizeFlipCards() {
    document.querySelectorAll('.flip-card').forEach(card => {
      const front = card.querySelector('.flip-face.front');
      const back = card.querySelector('.flip-face.back');
      if (!front || !back) return;

      const wasFlipped = card.classList.contains('is-flipped');
      card.classList.remove('is-flipped');

      const frontH = front.scrollHeight;
      const backH = back.scrollHeight;

      const MIN = 320;
      const MAX = 460;
      const h = Math.max(MIN, Math.min(Math.max(frontH, backH), MAX));

      if (card instanceof HTMLElement) {
        card.style.height = h + 'px';
      }

      if (wasFlipped) card.classList.add('is-flipped');
    });
  }


  // Initialize
  window.addEventListener('load', () => { 
    attachFlipEvents(); 
    sizeFlipCards(); 
  });

  function debounce<T extends (...args: any[]) => void>(
  fn: T,
  wait = 150
): (...args: Parameters<T>) => void {
  let t: number | undefined;
  return (...args: Parameters<T>) => {
    if (t !== undefined) window.clearTimeout(t);
    t = window.setTimeout(() => fn(...args), wait);
  };
}
  
  window.addEventListener('resize', debounce(sizeFlipCards, 150));

  const svcToggle = document.getElementById('services-toggle');
  svcToggle?.addEventListener('click', () => {
    setTimeout(() => { 
      attachFlipEvents(); 
      sizeFlipCards(); 
    }, 310);
  });
</script>

    <!-- ======= STYLES ======= -->
    <style>
      @import "tailwindcss";

      /* Custom CSS for enhanced styling */
      /* ---- GLOBAL LAYOUT FIXES ---- */
*, *::before, *::after { box-sizing: border-box; }  /* bring back Tailwind preflight's box model */

/* ---- CONTACT FORM CONTAINER ---- */
#contact #contact-form {
  overflow: hidden;                 /* clip any glows/edges that try to escape */
  border-radius: 1.5rem;            /* matches rounded-3xl */
}

/* Ensure all form controls & buttons stay inside their cells */
#contact #contact-form .form-input,
#contact #contact-form button,
#contact #contact-form a.btn-secondary {
  display: block;
  width: 100%;
  max-width: 100%;
  box-sizing: border-box;
}

/* Grid columns that never overflow (fixes first/second row overlap) */
#contact #contact-form .grid {
  grid-template-columns: repeat( var(--cols,1), minmax(0,1fr) );
}

/* At md+ your existing md:grid-cols-2 becomes 2 equal columns with proper minmax */
@media (min-width: 768px){
  #contact #contact-form .grid { --cols: 2; }
}

/* Select polish so its arrow doesn’t push the border */
#contact select.form-input {
  -webkit-appearance: none;
  appearance: none;
  padding-right: 2.5rem;
  background-image: none; /* keep native arrow; remove if you add a custom one */
}

/* Textarea comfort */
#contact textarea.form-input {
  min-height: 7.5rem;
  resize: vertical;
}

      
      /* Glass morphism effects */
      .glass-nav {
        backdrop-filter: blur(16px);
        background: rgba(15, 17, 21, 0.85);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      }

      /* === Services: compact sizing === */
#services{
  /* tighten everything a notch */
  --svc-pad: clamp(18px, 1.8vw, 26px);
  --svc-gap: clamp(10px, 1.2vw, 16px);
  --svc-icon: clamp(56px, 6vw, 72px);   /* avatar box */
}

/* slightly smaller titles inside cards (overrides Tailwind .text-xl) */
#services .flip-face h3,
#services .service-card h3{
  font-size: clamp(1.10rem, 1.45vw, 1.35rem);
  line-height: 1.25;
  letter-spacing: -0.01em;
  margin: 0; /* keep gap-driven rhythm */
}

/* body copy a touch tighter */
#services .flip-face p,
#services .service-card p{
  font-size: clamp(.95rem, 1.05vw, 1.05rem);
  line-height: 1.55;
}

/* smaller CTA and max width so it doesn’t look oversized */
#services .learn-more-btn{
  padding: .6rem .9rem;
  font-size: .95rem;
  border-radius: .75rem;
  width: min(360px, 100%);
}

/* slightly smaller icon glyph inside avatars (Lucide) */
#services .avatar-red svg,
#services .avatar-amber svg,
#services .avatar-indigo svg,
#services .avatar-rose svg,
#services .avatar-slate svg,
#services .avatar-green svg{
  width: 32px; height: 32px; /* was ~36–40 */
}

/* optional: tighten card corner radii just a hair */
/* slightly tighter cards */
#services .service-card,
#services .flip-face{
  padding: clamp(18px, 1.6vw, 24px);
}

/* compact CTA */
#services .learn-more-btn{
  padding: .6rem .9rem;
  font-size: .95rem;
  border-radius: .75rem;
  width: min(340px, 100%);
}



      .glass-dark {
        backdrop-filter: blur(16px);
        background: rgba(15, 17, 21, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      /* Typography */
      .hero-title {
        font-size: clamp(2.5rem, 8vw, 6rem);
        font-weight: 900;
        line-height: 1.1;
        letter-spacing: -0.02em;
      }

      .h2 {
        font-size: clamp(2rem, 5vw, 3.5rem);
        font-weight: 800;
        line-height: 1.2;
        letter-spacing: -0.01em;
      }

      .lead {
        font-size: 1.25rem;
        line-height: 1.6;
        color: #9CA3AF;
      }

      /* Bottom nav wrapper slides only on Y (no X jitter) */
#mobile-bottom-nav{
  transform: translateY(0);
  transition: transform .28s ease;
  will-change: transform;
}

/* Hidden state when scrolling down */
#mobile-bottom-nav.hide{
  transform: translateY(calc(100% + 24px));
}

/* Pill look (you already have most of this; a couple of stability tweaks) */
.mobile-pill-nav{
  display:flex; align-items:center; gap:.75rem;
  padding:.5rem .6rem;
  background: rgba(255,255,255,.92);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(0,0,0,.08);
  border-radius: 1.25rem;
  box-shadow: 0 16px 40px rgba(0,0,0,.35);
  max-width: calc(100% - 24px);    /* avoid overflow on tiny screens */
  flex-wrap: nowrap;
}

/* Icon buttons */
.bottom-nav-icon{
  width:3rem; height:3rem;
  display:flex; align-items:center; justify-content:center;
  border-radius:.875rem;
  color:#111827; opacity:.88;
  transition: transform .18s ease, opacity .18s ease, background .18s ease, color .18s ease;
  text-decoration:none;
}
.bottom-nav-icon:hover{ opacity:1; transform: translateY(-1px); background: rgba(17,24,39,.06); }
.bottom-nav-icon.active{ color:#DC2626; background: rgba(220,38,38,.10); }

/* Emphasize call */
.bottom-nav-icon.call{
  background: linear-gradient(135deg,#DC2626,#B91C1C);
  color:#fff !important;
  box-shadow: 0 10px 26px rgba(220,38,38,.45);
  border-radius: 1rem;
}

/* Pop animation goes on the PILL (child), not the wrapper */
@keyframes popIn { 0%{ transform: scale(.96); } 100%{ transform: scale(1); } }
.mobile-pop { animation: popIn .18s ease-out; }

/* Keep content clear of the bar */
@media (max-width: 1024px){
  body{ padding-bottom: 86px; }
}


      /* Buttons */
      .btn-primary {
        background: linear-gradient(135deg, #DC2626, #B91C1C);
        color: white;
        padding: 0.75rem 1.5rem;
        border-radius: 0.75rem;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
        box-shadow: 0 4px 20px rgba(220, 38, 38, 0.4);
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 30px rgba(220, 38, 38, 0.6);
      }

      .btn-primary-sm {
        background: linear-gradient(135deg, #DC2626, #B91C1C);
        color: white;
        padding: 0.5rem;
        border-radius: 0.75rem;
        display: inline-flex;
        align-items: center;
        transition: all 0.3s ease;
      }

      /* Fix: keep all service avatars perfectly square */
.avatar-red,
.avatar-amber,
.avatar-indigo,
.avatar-rose,
.avatar-slate,
.avatar-green{
  display: grid;
  place-items: center;
  width: 4rem;
  aspect-ratio: 1 / 1;   /* force square */
  height: auto;          /* let aspect-ratio drive height */
  min-width: 4rem;
  min-height: 4rem;
  border-radius: 1rem;
  flex: 0 0 auto;        /* don't stretch/shrink in flex */
  transform: translateZ(0); /* isolate from 3D perspective rounding */
}

/* Make sure the icon inside doesn’t get resized oddly */
.avatar-red  svg,
.avatar-amber svg,
.avatar-indigo svg,
.avatar-rose  svg,
.avatar-slate svg,
.avatar-green svg{
  width: 2rem;
  height: 2rem;
  flex: none;
}

      .btn-secondary {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.15);
        color: white;
        padding: 0.75rem 1.5rem;
        border-radius: 0.75rem;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
      }

      .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(220, 38, 38, 0.5);
        color: #F87171;
      }

      .glass-btn {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.15);
        color: white;
        transition: all 0.3s ease;
      }

      /* Lighter, more translucent sticky footer behind the button */
.back-footer{
  background: linear-gradient(
    to top,
    rgba(11,18,32,.40),
    rgba(11,18,32,.16) 45%,
    rgba(11,18,32,0) 75%
  );
  padding: .75rem .25rem 1rem;
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}
/* --- Back area behind the button (soft overlap) --- */
.back-footer{
  margin-top: auto;                     /* pin to bottom of the card */
  display: flex;
  justify-content: center;
  padding: .75rem .75rem 1.1rem;

  background: linear-gradient(
    to top,
    rgba(11,18,32,.28),
    rgba(11,18,32,.10) 50%,
    rgba(11,18,32,0) 80%
  );
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}

/* --- Glassy Back button --- */
.back-btn{
  appearance: none;
  -webkit-appearance: none;
  display: inline-flex;
  align-items: center;
  gap: .5rem;

  padding: .9rem 1.2rem;
  border-radius: 1rem;
  width: min(520px, 100%);              /* big, easy target; responsive */

  color: #E5E7EB;                       /* make text & icon light */
  font-weight: 700;
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(255,255,255,.16);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);

  /* no outer drop shadow */
  box-shadow: inset 0 1px 0 rgba(255,255,255,.08);
}

#svc-grid { align-items: start; }


/* Icon follows text color */
.back-btn svg{ flex-shrink: 0; }

/* States */
.back-btn:hover{
  background: rgba(255,255,255,.07);
  border-color: rgba(239,68,68,.28);
}
.back-btn:active{ transform: translateY(1px); }
.back-btn:focus-visible{
  outline: none;
  box-shadow: 0 0 0 2px rgba(239,68,68,.25);
}

/* let the button hang out of the card */
.flip-card,
.flip-face { overflow: visible; }

/* remove the grey/blur bar */
.back-footer{
  background: transparent;
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
  margin-top: 1rem;           /* space above the button */
  padding: 0 0 2.2rem;        /* reserve room for the overhang */
  position: relative;
  display: flex;
  justify-content: center;
}

/* center the button and push it down to overlap */
.back-btn{
  position: absolute;
  left: 50%;
  bottom: -0.8rem;            /* controls how much it overlaps */
  transform: translateX(-50%);
}

/* (optional) smaller overlap on very small screens */
@media (max-width: 420px){
  .back-btn{ bottom: -0.6rem; }
}

/* 1) Move it UP (less overlap) */
.back-btn{ bottom: .2rem; }     /* was -0.8rem */
.back-footer{ padding-bottom: 1.1rem; }  /* trim the reserved space */

/* 2) Keep it centered while pressing (no jump to the right) */
.back-btn:active{
  /* include the X-translate so we don't lose centering on mousedown */
  transform: translate3d(-50%, 1px, 0);
}

/* (optional hardening) */
.back-btn{ will-change: transform; }  /* smoother press animation */




      .glass-btn:hover {
        background: rgba(255, 255, 255, 0.1);
      }

      @media (max-width: 1024px){
  body{ padding-bottom: calc(86px + env(safe-area-inset-bottom)); }
}


/* Icon buttons (icons only) */
.bottom-nav-icon{
  width:3rem; height:3rem;
  display:flex; align-items:center; justify-content:center;
  border-radius:.875rem;
  color:#111827;                /* gray-900 for good contrast on white */
  opacity:.85;
  transition: transform .18s ease, opacity .18s ease, background .18s ease, color .18s ease;
  text-decoration:none;
}

.bottom-nav-icon:hover{ opacity:1; transform: translateY(-1px); background: rgba(17,24,39,.06); }
.bottom-nav-icon.active{ color:#DC2626; background: rgba(220,38,38,.10); }

/* Emphasize the call button */
.bottom-nav-icon.call{
  background: linear-gradient(135deg,#DC2626,#B91C1C);
  color:#fff !important;
  box-shadow: 0 10px 26px rgba(220,38,38,.45);
  border-radius: 1rem;
}


/* Pop animation when returning on scroll up */
@keyframes popIn {
  0% { transform: scale(.96); }
  100% { transform: scale(1); }
}
.mobile-pop { animation: popIn .18s ease-out; }

/* Keep content from sitting under the bottom nav */
@media (max-width: 1024px){
  body{ padding-bottom: 86px; } /* ~pill height + spacing */
}


      /* Chips and badges */
      .chip {
        background: rgba(220, 38, 38, 0.1);
        border: 1px solid rgba(220, 38, 38, 0.3);
        color: #F87171;
        padding: 0.5rem 1rem;
        border-radius: 2rem;
        font-size: 0.875rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }

      .badge-24h {
        background: rgba(220, 38, 38, 0.15);
        border: 1px solid rgba(220, 38, 38, 0.4);
        color: #EF4444;
        padding: 0.5rem 1rem;
        border-radius: 2rem;
        font-size: 0.875rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }

      .badge-local {
        background: rgba(220, 38, 38, 0.1);
        border: 1px solid rgba(220, 38, 38, 0.3);
        color: #F87171;
        padding: 0.5rem 1rem;
        border-radius: 2rem;
        font-size: 0.875rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }

      /* Navigation */
      .nav-link {
        padding: 0.5rem 1rem;
        border-radius: 0.75rem;
        color: #9CA3AF;
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
        z-index: 10;
      }

      .nav-link:hover,
      .nav-link.active {
        color: #EF4444;
      }

      .mobile-nav-link {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1rem;
        border-radius: 0.75rem;
        color: #9CA3AF;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .mobile-nav-link:hover {
        background: rgba(255, 255, 255, 0.05);
        color: #EF4444;
      }

      /* Stats cards */
      .stat-card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1.5rem;
        padding: 1.5rem;
        text-align: center;
        transition: all 0.3s ease;
      }

      .stat-card:hover {
        transform: translateY(-4px);
        background: rgba(255, 255, 255, 0.08);
      }

      /* Service cards */
      .service-card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1.5rem;
        padding: 2rem;
        transition: all 0.3s ease;
        height: fit-content;
      }

      /* 3D flip cards */
.flip-card { 
  position: relative;
  perspective: 1200px;                 /* depth for the 3D effect */
  height: auto;                        /* we’ll set exact height via JS */
}

.flip-inner {
  position: relative;
  width: 100%;
  height: 100%;
  transform-style: preserve-3d;
  transition: transform .7s cubic-bezier(.2,.7,.2,1);
}

.flip-card.is-flipped .flip-inner { 
  transform: rotateY(180deg);
}

/* Faces share the same look as your service-card content */
.flip-face {
  position: absolute;
  inset: 0;
  padding: 2rem;                        /* matches .service-card */
  border-radius: 1.5rem;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  backface-visibility: hidden;
  display: flex;
  flex-direction: column;
}

.flip-face.back {
  transform: rotateY(180deg);
  /* subtle difference to hint it’s the back (optional) */
  background: rgba(255, 255, 255, 0.06);
  border-color: rgba(220, 38, 38, 0.25);
}

/* When reduced motion is preferred, just crossfade */
@media (prefers-reduced-motion: reduce) {
  .flip-inner { transition: none; }
  .flip-card.is-flipped .front { display: none; }
  .flip-card.is-flipped .back { position: relative; transform: none; }
  .flip-face { backface-visibility: visible; }
}


      .service-card:hover {
        transform: translateY(-4px);
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(220, 38, 38, 0.3);
      }

      .card-content {
        height: 100%;
        display: flex;
        flex-direction: column;
      }

      .learn-more-btn {
        background: rgba(220, 38, 38, 0.1);
        border: 1px solid rgba(220, 38, 38, 0.3);
        color: #F87171;
        padding: 0.75rem 1rem;
        border-radius: 0.75rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        margin-top: auto;
        transition: all 0.3s ease;
      }

      .learn-more-btn:hover {
        background: rgba(220, 38, 38, 0.2);
        color: #EF4444;
      }

      .service-checklist {
        list-style: none;
        padding: 0;
      }

      .service-checklist li {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        color: #D1D5DB;
        font-size: 0.875rem;
        line-height: 1.4;
      }

      /* Avatar styles */
      .avatar-red { 
        background: linear-gradient(135deg, #EF4444, #DC2626);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .avatar-amber { 
        background: linear-gradient(135deg, #F59E0B, #D97706);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .avatar-indigo { 
        background: linear-gradient(135deg, #6366F1, #4F46E5);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .avatar-rose { 
        background: linear-gradient(135deg, #F43F5E, #E11D48);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .avatar-slate { 
        background: linear-gradient(135deg, #64748B, #475569);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .avatar-green { 
        background: linear-gradient(135deg, #10B981, #059669);
        width: 4rem;
        height: 4rem;
        border-radius: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      /* Pill button */
      .pill-button {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.15);
        color: white;
        padding: 0.75rem 1.5rem;
        border-radius: 2rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
      }

      .pill-button:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(220, 38, 38, 0.5);
        color: #F87171;
      }

      /* About features */
      .about-feature {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
      }

      .feature-icon {
        background: linear-gradient(135deg, #DC2626, #B91C1C);
        width: 3rem;
        height: 3rem;
        border-radius: 0.75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
      }

      /* Reviews */
      .review-card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        padding: 1.5rem;
      }

      .review-nav-btn {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        background: rgba(0, 0, 0, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: white;
        width: 2.5rem;
        height: 2.5rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        opacity: 0;
      }

      .review-nav-btn:hover {
        background: rgba(220, 38, 38, 0.8);
      }

      .glass-dark:hover .review-nav-btn {
        opacity: 1;
      }

      @media (max-width: 768px) {
        .review-nav-btn {
          opacity: 1;
        }
      }

      /* FAQ */
      .faq-item {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        overflow: hidden;
      }

      .faq-question {
        padding: 1.5rem;
        font-weight: 600;
        color: white;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: space-between;
        list-style: none;
      }

      .faq-question::-webkit-details-marker {
        display: none;
      }

      .faq-answer {
        padding: 0 1.5rem 1.5rem;
        color: #D1D5DB;
        line-height: 1.6;
      }

      /* Contact form */
      .contact-method {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        transition: all 0.3s ease;
        text-decoration: none;
      }

      .contact-method:hover {
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(220, 38, 38, 0.3);
      }

      .contact-icon {
        background: linear-gradient(135deg, #DC2626, #B91C1C);
        width: 3rem;
        height: 3rem;
        border-radius: 0.75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
      }

      .form-group {
        position: relative;
      }

      .form-input {
        width: 100%;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.15);
        border-radius: 0.75rem;
        padding: 1rem;
        color: white;
        font-size: 1rem;
        transition: all 0.3s ease;
      }

      .form-input:focus {
        outline: none;
        border-color: rgba(220, 38, 38, 0.5);
        background: rgba(255, 255, 255, 0.08);
      }

      .form-input::placeholder {
        color: #9CA3AF;
      }

      .hidden-field {
        position: absolute;
        left: -9999px;
        opacity: 0;
        height: 0;
        width: 0;
      }

      /* Success animation */
      .success-message {
        text-align: center;
        padding: 2rem;
      }

      .success-checkmark {
        width: 80px;
        height: 80px;
        margin: 0 auto 1rem;
      }

      .success-animation {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        stroke: #10B981;
        stroke-width: 2;
        stroke-miterlimit: 10;
        box-shadow: inset 0px 0px 0px #10B981;
        animation: fill-green 0.4s ease-in-out 0.4s forwards, scale 0.3s ease-in-out 0.9s both;
      }

      .success-animation circle {
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        stroke-width: 2;
        stroke-miterlimit: 10;
        stroke: #10B981;
        fill: none;
        animation: stroke-circle 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
      }

      .success-animation path {
        transform-origin: 50% 50%;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        animation: stroke-check 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
      }

      @keyframes stroke-circle {
        100% {
          stroke-dashoffset: 0;
        }
      }

      @keyframes stroke-check {
        100% {
          stroke-dashoffset: 0;
        }
      }

      @keyframes fill-green {
        100% {
          box-shadow: inset 0px 0px 0px 30px #10B981;
        }
      }

      @keyframes scale {
        0%, 100% {
          transform: none;
        }
        50% {
          transform: scale3d(1.1, 1.1, 1);
        }
      }

      /* Social buttons */
      .social-btn {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.15);
        color: white;
        width: 3rem;
        height: 3rem;
        border-radius: 0.75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
      }

      .social-btn:hover {
        background: rgba(220, 38, 38, 0.1);
        border-color: rgba(220, 38, 38, 0.3);
        color: #F87171;
      }

      /* Footer */
      .footer-link {
        color: #9CA3AF;
        transition: color 0.3s ease;
        text-decoration: none;
      }

      .footer-link:hover {
        color: #F87171;
      }

      /* Animations */
      .animate-fade-in {
        animation: fadeIn 0.5s ease-out forwards;
      }

      .animate-slide-in {
        animation: slideIn 0.3s ease-out forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes slideIn {
        from {
          transform: translateX(100%);
        }
        to {
          transform: translateX(0);
        }
      }

      /* Full-bleed page — remove default margins and any horizontal overflow */
html, body {
  margin: 0;           /* <-- removes the 8px gutter */
  padding: 0;
  width: 100%;
  max-width: 100%;
  overflow-x: clip;    /* or hidden if you need broader support */
}


      /* Responsive adjustments */
      @media (max-width: 768px) {
        .service-card {
          padding: 1.5rem;
        }
        
        .hero-title {
          font-size: clamp(2rem, 8vw, 4rem);
        }
        
        .section {
          padding-top: 3rem;
          padding-bottom: 3rem;
        }
      }

      /* Smooth scrolling */
      html {
        scroll-behavior: smooth;
      }

      /* Loading states */
      .loading {
        opacity: 0.6;
        pointer-events: none;
      }

      /* Focus states for accessibility */
      .btn-primary:focus,
      .btn-secondary:focus,
      .form-input:focus {
        outline: 2px solid #F87171;
        outline-offset: 2px;
      }

      /* High contrast mode support */
      @media (prefers-contrast: high) {
        .glass-nav,
        .glass-dark,
        .service-card,
        .stat-card {
          background: rgba(0, 0, 0, 0.9);
          border-color: rgba(255, 255, 255, 0.5);
        }
      }

      /* Reduced motion support */
      @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
        }
      }

      /* Stop Safari from hard-clipping glass cards on the right edge */
html, body { overflow-x: hidden; }   /* Replace your previous overflow-x: clip */

/* Make the two columns in the About grid shrink properly on small screens */
#about .grid > * { min-width: 0; }

/* Keep the stats/reviews glass card from creating an accidental overflow */
#about .glass-dark { overflow: visible; max-width: 100%; }

    </style>
  </body>
</html>
